const recursive = () => {};

export default recursive;

// vars = maze,path,winconditions
// const path = path || {traversed,queued,exhausted}

// if win on currentNode return path else
// run the traversal algorithm
// return recusrive(maze,path,winconditions)
